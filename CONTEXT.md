# CONTEXT — 詞彙表

這份檔案只放詞彙定義。實作細節、決策理由、色值清單都不放這裡。

---

## 色彩

**中性色階（tonal scale）**
表達層級與遠近的一整排顏色，同色相、不同明度。要求是**差異小而規律**——相鄰兩階要看得出不同，但整排要像同一個家族。背景、卡底、邊框、內文都取自這排。

**類別色（categorical）**
用來區分圖表裡不同序列的顏色。要求與中性色階**相反**：彼此差異要盡量大，且在色盲模擬下仍然分得開。類別色不表達大小或順序——誰在前誰在後沒有意義。

**示意色（illustrative）**
文章裡「紅色」「藍紫色」這類**被上色的詞**所用的顏色。它的職責是讓那個字看起來像它說的顏色，所以色相由詞義決定，不由系統決定。

**連續色帶（sequential）**
熱圖用的漸層。表達的是量值大小，所以明度必須單調變化——讀者是靠亮暗判斷大小的。

**階（step）**
色階上的一個位置，以數字命名（50 最暗、900 最亮）。說「比邊框深一階」指的是這個。

**錨點（anchor）**
色階或尺度兩端被固定住的值。錨點通常是已經拍板、不打算再動的顏色或字級，其餘的值由它們推導出來。

**白名單（palette whitelist）**
允許出現在產物裡的完整顏色清單。不在清單上的顏色一律視為違規，由驗證腳本攔下。

**辨識 vs 份量**
一個通道能做的兩件不同的事。**辨識**是「認得出這個跟旁邊不一樣」——它有門檻、量得到（對比度、可辨識度 ΔEok），過不了門檻就是沒做到，跟好不好看無關。**份量**是「這個東西在畫面上有沒有存在感」——它量不到，只有人看得出來，判準是理念二（低調但不失高級感）。

**同一個東西可以辨識成立而份量不足**，反之亦然，所以驗收要分開問：問「分不分得出」得到的是辨識，問「好不好看」得到的是份量。**量測綠只證明辨識成立，證明不了份量夠**（2026-07-28 目視驗收實例：對比全面改善的九處改動，本人的評語是「太素」）。

**文字通道 vs 非文字通道**
同一個顏色用在**字**上，與用在**框線、底線、底色、焦點框**上，受不同規則約束。字的通道窄——顏色在那裡要跟字重、字體、明度搶同一個位置，而且對比門檻高（4.5 對 3.0）。所以一個顏色可以在非文字通道上到處出現而不失焦，在文字通道上卻只能守住少數幾個位置。**說「這個顏色用在哪裡」時，必須先講是哪個通道**。

---

## 版面角色

**署名（signature）**
內容區裡**自報身分**的那一塊——名字與定位句。它不是主角也不是配角：**主角與配角在爭同一份注意力預算，署名不參與那場爭奪**。理由是它回答的問題跟它們不同——主角回答「這個人能做什麼」，署名回答「這是誰」，而後者沒有被回答的話，前者是誰做的都不知道。

**全站只有一個實例，在首頁。**導覽列品牌與頁尾版權雖然也寫著同一個名字，**不是署名**——判斷法沿用「版面 vs 內容」：拿掉導覽列品牌，讀者少的是一個回首頁的入口（版面）；拿掉首頁那一塊，讀者少的是「這是誰的站」這份資訊（內容）。所以那兩個不拿署名的豁免，它們照版面元件的規則走。

（2026-07-30 本人拍板。憲法第一章原本只有主角與配角兩種角色，名字因此被歸進「文字元素／15 秒」那一格而降階——「首頁整個像專案展示品」是照那張表做出來的結果，不是失手。）

---

## 排版

**尺度（type scale）**
所有字級的來源。定一個基準與一個比例，其餘字級都是「基準 × 比例的次方」，不逐一挑選。

**比例（ratio）**
尺度裡相鄰兩階的倍數關係。這個站的比例來自「主標約為內文 5 倍」除以七階。

**標籤（label）**
說明「接下來這一塊是什麼」的短詞。**表格的欄名列名與卡片的前導小標是同一種東西**（2026-07-28 合併；此前分成「短標籤」與「表頭」兩組樣式，字級字距相同、只差字重與字體，但職責分不出差別）。標籤不是內容的一部分——拿掉它，讀者少的是一個入口，不是一份資訊。

**排版樣式（type style）**
字級、行高、字距、字重**綁在一起**的一組設定。選一個樣式，四個屬性一次到位——拆開來設是「設了字級忘了設行高」這類缺陷的來源。**「標籤」那一組是唯一的例外，它綁五個**（2026-07-28 合併時多收了字體）：那一組的襯線是判準的一部分——說明「接下來這一塊是什麼」的短詞一律襯線 600，落點有 11 個，分散宣告就等於 11 個要同步的地方。其餘幾組的字體仍留在各自的元件規則裡。

**行長（measure）**
一行文字的寬度，以字數計。中文以漢字數表示（漢字為全角，一字＝1em）。

**流動字級（fluid type）**
字級隨視窗寬度連續變化，而非在斷點跳動。這個站的手機與桌機各有一套等比尺度，之間線性內插。

---

## 間距

**垂直節奏（vertical rhythm）**
讓所有垂直間距都是同一條基線的倍數或簡單分數，使不同區塊的文字落在同一組看不見的橫線上。

**基線（baseline）**
垂直節奏的基本單位，等於內文的行高。

**版面間距 vs 元件內間距**
版面間距是區塊與區塊之間的距離，受垂直節奏約束。元件內間距是按鈕內距、圖示間隙這類尺寸，**不受節奏約束**——它們不落在文字基線上。

---

## 動效

**過場（transition）**
回應狀態改變而發生的動作——hover、進場、換頁。這個站上所有的動作都屬於這一類。

**紅線**
判定「過度」的界線。它不是一個好看的比例，是一條會讓違規變成錯誤的線。動效有三條，量的是不同的東西：一條管**單一動作有多顯眼**（時長），一條管**使用者等多久**（延遲加時長的總和），一條管**一個一直在跑的東西有多容易把視線拉走**（峰值速度）。

**常駐動態（standing motion）**
沒有起點也沒有終點、頁面開著就一直在跑的動作。它不是過場——沒有人在等它，也沒有「這一次多久」可以量，所以管過場的紅線對它沒有定義。它的問題是「一直在」。

**峰值速度**
常駐動態裡任何一點在任何一刻的最快移動速度。取峰值不取平均，是因為一個平均很慢但偶爾抽動一下的東西，被看見的就是抽動的那一下。量法是**參數的解析上界**，不是目測也不是實測幀。

**檔（tier）**
動效的離散取值位置——時長三檔、位移三檔。與色彩的「階」不同：階要求規律遞進，檔只要求彼此差得夠遠。兩個相差在辨識門檻以內的值不構成兩檔，那是雜訊。

**辨識門檻**
兩個值要差多少，人才分得出來。時距與空間量的門檻都是比例而非絕對值（約一成）。這個站用它來判斷「值得不值得為了一個差別去做遷移」。

**回彈（overshoot）**
動作衝過終點再彈回來。它是緩動曲線裡唯一帶材質性格的參數，讀起來是「被推了會彈」。這個站不用它。

**進場起點 vs 常駐態**
同一組視覺參數（位移、模糊、透明度）可以是動作的起點，也可以是元素平時就長那樣。前者是一瞬間，後者是持續狀態——**核准了前者不等於核准了後者**。

**冗餘提示**
同一件事由多個管道傳達（圖示、游標、文字）。提示已經足夠時，再加一個動畫不增加資訊，那是裝飾。

**降低動態偏好（reduced motion）**
訪客在系統層表明不要動態。要關掉的是**空間位移**——移動、縮放、模糊；淡化與顏色變化不但不必關，它們正是規範建議的替代方案。全部關掉會讓畫面變成硬跳，反而更糟。

**聚焦組（focus group）**
專案內頁裡「讀者一次該看的那一塊」。一組通常是引言段＋圖＋解讀段，或依論點的 1–3 段文字。只要有一部分落在焦點帶內，整組就清晰；其餘的組常駐在糊化降權的狀態。它讓作者能放心把字寫長——**同一屏的字沒有變少，看起來的字變少了**。分組是作者的判斷，由人標記而非規則推斷：規則猜錯的方向不可控，而少含一段的代價（圖清晰、解說糊掉）正是這個機制要避免的事。

**焦點帶**
視窗中央那一條決定「誰清晰」的橫帶，這個站取中央 30%。帶寬要與組高同量級：太窄會讓一組還沒讀完就糊掉，太寬則同時清晰的組太多、等於沒有機制。它是聚焦組唯一的判定依據——不看捲動方向，也不看讀了多久。

---

## 背景設計

**背景設計（motif）**
畫在全站背景上、用統計語彙表達本人的那個圖案。它不取材自任何一篇專案的真實資料——它講的是這個人，不是這個人的某份研究。
舊稱「母題」。`DECISIONS.md` 與 `.scratch/` 是標了日期的歷史紀錄，仍用舊名；程式識別字維持 `motif`（元件 `Motif.astro`、`motif=` 屬性、CSS 變數）。

**技法 vs 有故事的元素**
定案門檻把元素分兩類登記：**有故事的元素**講得出它在說什麼；**技法**是為了讓有故事的元素看起來成立或跑得涼而存在的手段。技法必須明白登記為技法，**不准替它編故事**——編出來的故事比沒有故事更糟。

**密度**
每千平方像素幾個點。背景設計的點數一律用密度表示，不用絕對數量——寫死數量會讓同一個設計在大螢幕上變稀疏、小螢幕上變擠。

**磚（tile）**
閱讀頁的圖案定義在一塊固定高度的磚裡，繪製時按捲動位置重複貼。磚首尾同源，所以接縫在數學上不存在，頁面可以無限長。

**文字帶**
畫面上文字可能出現的區域。背景在這一帶要比別處收斂——亮度上限、密度都比其他地方嚴，因為字要是最清楚的東西。

**可見元素**
背景上一塊合成後不透明度跨得過門檻的連通區域。**一顆點是一個，整條趨勢線也是一個**——
數的是「眼睛分得出來的東西」，不是擲了幾個點。兩者差很多：擲出去的點會被文字帶減光、
四周化開、以及自身的柔邊壓到門檻以下，實測只有三到五成留得下來。
**凡是講「這裡夠不夠密」，都要用可見元素講，不能用點數講**——用點數講會系統性高估。

**均勻判準**
「背景不該在畫面上留出整片空白」這件事的驗收方式：取一塊固定大小的方格在畫面上滑動，
只要文字帶沒有蓋過它一半以上，格子裡的可見元素就不得少於下限。
**這條判準只適用首頁**；閱讀頁的背景本來就該幾乎看不見，明文豁免（2026-07-28 本人裁定）。
它**進不了 CI**——閘門看不到畫布像素——所以它是驗收判準，不是閘門條款。

**每幀預算**
一個常駐動態每次重畫**最多**可以佔用的主執行緒時間。它是「這個站還擺得下多少東西」的
唯一換算基準：量到成本之後，能擺幾個點是除出來的，不是估的。
成本要拆成**固定開銷**（整面操作，隨畫布像素數變動）與**邊際成本**（每個點），
只量一個點位會把兩者混在一起，反推出來的容量必錯。

---

## 等高線場

**等高線場（field）**
第 10 版加的第五層背景。與背景設計同一個量級（元素小、覆蓋滿），差別在**它用一套語彙承載四條敘事**，而不是四種元素各自鋪滿——後者正是「各別都搔不到癢處，組合起來又很怪」的成因。

**斷口（crack）**
等高線中途不畫過去的那一段。**它是這一層的樞紐**：次級線只長在斷口旁、餘燼只落在斷口上——裂縫決定深度在哪，也決定能量在哪。四條敘事因此互相咬合，不是四條並排。

**次級線**
斷口旁邊長出來的短線。它表達的是「深度只在被檢查過的地方顯現」，所以**它不能自己出現在別處**——離開斷口就沒有那個意思了。

**餘燼（ember）**
落在斷口上、只改變不透明度而不移動的點。**只改不透明度不改位置**是它的定義，不是實作細節：因此它在「降低動態偏好」下**保留**，那個偏好要關的是空間位移。

**回彈（shock）**
捲動觸發整場輕微位移後回位。**位移發生在貼圖時，不是重新產生等高線**——目的座標加偏移即可。它受兩條紅線夾住：回位時間對動效紅線①，幅度 × 角速度對紅線③。

**比例不等於佔比**
場的幾個「比例」參數（裂縫比例、餘燼比例）是拿去跟一個**平滑值噪聲**比大小的門檻，
而那個噪聲的分布集中在 0.5——所以「裂縫比例 0.09」實際只斷 0.9% 的線段，不是 9%。
**凡是講這一層的密度，都要講量出來的條數，不能講參數值**：0.09 那組實測是斷 44 段、
次級線 3 條、餘燼 0 顆，四條敘事有兩條等於不存在，而參數表上完全看不出來。

**分桶（bucketing）**
把連續的不透明度量化成固定檔數、同一檔收進同一條路徑再各自描邊。
**它不是效能手段，是正確性手段**：`strokeStyle` 在路徑中途改對描邊無效，只吃最後一次
設的值，於是整層線都套用最後一段的透明度——文字帶減光會「寫了但完全沒生效」，
而畫面看起來完全正常。

**預先建場**
把整層畫進一張離屏畫布，每幀只貼圖。場只在**參數或視窗尺寸改變**時重建。
連帶產生一個要分開講的成本：**建場成本是一次性的**（改版面時付一次），
把它混進每幀成本的最大值，一個一次性的支出會被讀成「有時候會爆」。

---

## 響應式

**斷點（breakpoint）**
版面在某個寬度改變結構的位置。**只有結構改變（欄數、排列方式）才配得上一個斷點**；純尺寸變化一律流動處理——讀者的螢幕不會在某個像素值突然變成另一種裝置。

---

## 形狀

**形狀語彙是正交的，站上沒有任何「手做感」的形狀**（2026-07-29 盤點，本人問「這會不會變成風格縫合」時查的）
實際數過：傾斜／扭曲／不規則遮罩 **0 處**（唯一的 `clip-path` 是擦除比對器的功能性裁切，不是裝飾）；直線 1px／2px **46 處**；統一圓角 `--radius` **11 處**；柔邊 radial 暈 **3 處**（背景右上主色暈、左下暗暈、列聯表行標後面的墨渲染）。

判斷新元素會不會「不搭」，就看它落在哪一邊：**柔邊漸層是既有語彙**（第 4 個實例仍是同一種語言）；**有筆勢、有角度、上粗下細的手繪感是外來語**——它會是全站唯一一個，那就是縫合。這條同時是判準三的實例：暗暈講得出故事（背景那道暈滲進元件），毛筆講不出（站上沒有第二個書法元素可以呼應它），而**編出來的故事比沒有故事更糟**。

**通道（channel）**
一個元素用來表達狀態的那個屬性——文字亮度、文字色、邊框色、底色、位置。**hover 的回饋要用元素靜止態已經在用的通道**（2026-07-28）：靠亮度區分的就提高亮度，靠邊框成立的就動邊框。換一個新通道，等於在 hover 那一刻改變元素的說話方式。

一個元素**擁有**哪些通道是形狀決定的：一列表格只擁有底色（文字屬於儲存格、邊框屬於 `td`），所以它的 hover 只能是底色。全站唯一疊兩個通道的是卡片（邊框＋位移），理由見 `SPEC-motion-and-shape.md`。

**版面 vs 內容（這個設計系統的結構軸）**
2026-07-28 維度盤點輪**連續三次**撞到同一條界線，所以把它命名下來：**一個東西是「版面在框住內容」，還是「內容自己的一部分」**——兩邊適用相反的規則，而兩邊的理由都出自理念一。

| 維度 | 版面那側 | 內容那側 |
|---|---|---|
| 圓角 | 區塊邊界 → 修圓 | 表格格線 → 直角 |
| 邊框 | 框（`--color-border-strong`，閘門驗 3:1） | 線（`--color-border`，不驗） |
| 圖片比例 | 版面元件的圖 → 一律同比例 | 統計圖表 → 比例由資料決定 |

**判斷方法**：拿掉它，讀者是**少了一個容器的邊界**，還是**少了一份資訊**？後者就是內容那側。

**線 vs 框**
兩種一像素的線，差別不在粗細在**承載什麼**。**框**（`--color-border-strong`，對比 4.66，閘門驗 3:1）承載「這是一個獨立區塊」的辨識資訊——卡片、按鈕、標籤。**線**（`--color-border`，對比 2.35，閘門不驗）是分隔、規則、格線、軸線，不承載辨識資訊。

這個二分法**有第三種例外，2026-07-29 起存在**：用元素主色畫的線。它們承載的不是辨識也不是分隔，是**份量**——「這一塊是被設計過的」。目前四個落點：列聯表行標左緣那條 2px、擦除比對器最終版標籤的框，以及 2026-08-02 票 05 加的**導覽列底線與頁尾頂線**。四個都**沒有任何閘門在看**（對比檢查只驗文字色，不驗 `border-color`），已登記在 `REFERENCE-accent-text.md`「刻意不修的」那份清單上。

**一條線不夠明顯時要換的是色階，不是寬度**——加粗不會改變量得到的對比值，粗線只是量不到的補償，而這個站量不到的不算數。所以邊框寬度全站只有 1px 一檔（2026-07-28）。

**慣例符號 vs 要猜的符號**
符號能取代文字時是好事——**增加可視化就是增加易讀性**（理念一）。但那個好處只在讀者**一眼就懂**時成立：**慣例符號**（`#`＝標籤、`↔`＝可拖曳、`▶`＝可展開）可以單獨用；**要猜的符號**必須配上文字，或乾脆不用，因為猜的那一秒就是不易讀。限制數量的不是位元組（字元圖示是 0），是**注意力**。

**深度用顏色，不用光影**
這個站不用陰影（`box-shadow`／`text-shadow`／`drop-shadow` 一律零個，2026-07-28 登記為決定並設閘門）。陰影是在模擬一個光源與一個物理空間；這裡沒有物理空間，只有一張紙上的深淺。**深淺由色階講，可互動由邊框變色講**——同一件事不要兩種語言講，多的那種就是噪音。

**區塊邊界 vs 內容格線**
兩種看起來都是「線框」的東西，但職責相反，所以圓角規則相反。**區塊邊界**是把一塊東西從版面上圈出來的外緣（卡片、按鈕、可展開段落、嵌進文章的圖片）——它的角要修圓，讓邊界柔化、不像被刀切出來。**內容格線**是內容自己的骨架（表格的行列線）——它的工作是讓眼睛沿著行列掃，修圓會讓垂直對齊的視覺線斷開，所以維持直角。只有一條邊的分隔線不屬於任一類：**它沒有角**。

---

## 透明度

**顯隱 vs 層級**
`opacity` 的職責是讓整個元素或整層淡入淡出。**表達文字的層級是色階的工作**，不是透明度的——透明度會讓文字與背景混色，實際對比度因此偏離宣告值，驗證也就量不到真的東西。

---

## 展示櫃

**展示櫃（showcase）**
把站上**現行**的設計元素按維度分類陳列的一區。它陳列的是現在是什麼、依哪幾條理念決定，不是當時怎麼選的。這一點與 `/process/` 存檔正好相反——存檔的價值在不跟著現在變，展示櫃的價值在**就是現在**。所以它必須吃真 token，複製一份會讓它在換色之後說謊。

**敘述 vs 成品示範**
展示櫃每一項的兩半，只有一半會過期。**成品示範**吃真 token，站上改了它自己就變；**敘述**是純文字，站上改了它不會跟著變，從那一刻起就在說謊。同步規則管的只有敘述那一半。

**過期標記**
敘述與現行實作脫節時掛在該項上的**對外**標示。用對外語氣（「這一項的設計已更新，說明正在重新討論」），不用內部待辦語氣——這個站的立身之本是公開過程，「正在重新討論」證明的正是每個元素都真的被討論過。元素名與成品示範照常顯示，**不畫刪除線**：刪除線讀起來像元素被廢除，但實情是元素還在、只有說明過期。

**可增補容器**
展示櫃不是一篇要寫完的文章，是一個櫃子。空的格子只是還沒放東西，不是缺陷；但**對外不顯示空格**——沒有內容的分類不出現在目錄上。補一項就上架一項，來源是 `DIMENSIONS.md`：盤點表對內，展示櫃對外。

---

## 流程

**換色協議**
更換主題色時的標準程序。細節見 `CLAUDE.md`。

**雙源（deliberate duplication）**
`/process/` 那幾頁的樣式是 `global.css` 的獨立副本，刻意不共用。它們是標了日期的歷史重演存檔，隔離是特性不是缺陷；但換色時兩邊必須同步。

**接縫（seam）**
測試觀察系統的位置。這個站取最高的兩個：建置產物 `dist/`，以及圖表 PNG 的像素。
