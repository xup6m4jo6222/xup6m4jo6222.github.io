/**
 * 那句提示詞的單一來源（票 01）。
 *
 * **這不是文案，是證物**：2026-07-30 實驗真正送出的提示詞，一字未改。
 * 首頁展示卡與 AI 實作 #1 內頁都引用它——版面不共用（首頁是預告片、內頁是完整版），
 * 共用的只有這一個字串。內頁的 .md 裡另有一份字面值（markdown 讀不到模組），
 * 閘門第 14 類驗兩邊在產物裡逐字一致，分家就紅。
 *
 * 副檔名 .mjs：閘門（純 node）要 import 得到，理由同 categories.mjs。
 */
export const PROMPT =
	'我想做一個具有設計感的個人作品頁。我是林郁為，這個個人頁面想要放「統計分析」和「AI實作」這兩類的作品，首頁只放一些個人資訊，簡單為主。';
