---
/* 原型變體 C「心裡猜 — 不演」（機制 11 的最省版）
   PROTOTYPE：丟棄式程式。

   問句與圖直接並存，零動畫、零互動、零 JS。
   它存在的目的是當**對照組**：如果 A 的揭幕演出實際上沒有加分，那就該用這個——
   零位元組、零每幀 CPU、零相容性風險，而且問句該做的事（設定解讀框架）它照樣做到。

   「不演也一樣好」是一個要被認真對待的可能性，不是墊底的選項。

   文字是佔位，取自本人 2026-07-30 的口述草稿，最終文字由本人撰寫。 */
---

<section class="pc">
	<h1 class="pc-name">林郁為</h1>
	<p class="pc-tagline" data-slot="定位句｜我是誰、往哪走">統計背景，正朝資料科學邁進</p>

	<article class="pc-card">
		<p class="pc-ask" data-slot="問句｜同時是鉤子與解讀說明書">
			你知道，如果只跟 AI 說「我想做一個具有設計感的個人作品頁」，它會給你什麼成品嗎？
		</p>

		<figure class="pc-shot">
			<img src="/images/portfolio-site/rejected-v1.png" alt="AI 自己產出的版本" width="760" height="470" />
			<figcaption class="pc-note" data-slot="標註｜這張圖是什麼">這是 AI 給的第一版</figcaption>
		</figure>

		<p class="pc-exit">
			<a href="/projects/ai/portfolio-site/" data-slot="出口文字">那我做了什麼 →</a>
		</p>
	</article>

	<p class="pc-links">
		<a href="/projects/">Projects</a><span aria-hidden="true"> · </span><a
			href="https://github.com/xup6m4jo6222">GitHub</a
		><span aria-hidden="true"> · </span><a href="mailto:xup6m4jo6222@gmail.com">Email</a>
	</p>
</section>

<style is:inline>
	.pc {
		display: flex;
		flex-direction: column;
		gap: var(--sp-sm);
	}

	.pc-name {
		font-size: var(--fs-4);
		line-height: var(--lh-4);
		letter-spacing: var(--ls-display);
		font-weight: 400;
		margin: 0;
	}

	.pc-tagline {
		margin: 0;
		color: var(--color-text-muted);
	}

	.pc-card {
		margin-top: var(--sp-lg);
		display: flex;
		flex-direction: column;
		gap: var(--sp-md);
	}

	.pc-ask {
		margin: 0;
		font-size: var(--fs-2);
		line-height: var(--lh-2);
		max-width: 34em;
	}

	.pc-shot {
		margin: 0;
		display: flex;
		flex-direction: column;
		gap: var(--sp-sm);
	}

	.pc-shot img {
		display: block;
		width: 100%;
		height: auto;
		max-height: 300px;
		object-fit: cover;
		object-position: top;
		border: 1px solid var(--color-border-strong);
		border-radius: var(--radius);
	}

	.pc-note {
		color: var(--color-text-muted);
	}

	.pc-exit {
		margin: 0;
	}

	.pc-links {
		margin: var(--sp-lg) 0 0;
		color: var(--color-text-muted);
	}
</style>
