---
/* 原型變體 B「心裡猜 — 點了才揭曉，卡片式」（機制 11 的第 1 層版）
   PROTOTYPE：丟棄式程式。

   2026-07-30 第二輪修正（本人回饋）：
   ① **公告必須是卡片式**——有框、有底。理由不是裝飾，是**限界**：不加框的公告會跟
      名字同一個層級，把首頁的注意力整個吃掉，背景設計（承載「我是誰」的那一層）
      就沒有位置了。框把公告從「頁面」降回「頁面上的一塊」。
   ② **左上角明確標「最近專案」**——標籤同樣是降格裝置，它宣告這是一個區塊。
   ③ 層級隨之改變：**主角是「名字＋背景設計」，公告是一塊有邊界的次要區塊。**
      所以名字回到階 6（大），問句降到階 1（跟內文同級），不再跟名字搶。

   文字是佔位，取自本人 2026-07-30 的口述草稿，最終文字由本人撰寫。 */
---

<section class="pb">
	<h1 class="pb-name">林郁為</h1>
	<p class="pb-tagline" data-slot="定位句｜我是誰、往哪走">統計背景，正朝資料科學邁進</p>

	<article class="pb-card">
		<p class="pb-eyebrow">最近專案</p>

		<p class="pb-ask" data-slot="問句｜同時是鉤子與解讀說明書">
			你知道，如果只跟 AI 說「我想做一個具有設計感的個人作品頁」，它會給你什麼成品嗎？
		</p>

		<div class="pb-gate">
			<button type="button" class="pb-btn" data-reveal-btn data-slot="按鈕文字">
				看 AI 給了什麼
			</button>
		</div>

		<figure class="pb-shot" data-curtain hidden>
			<img src="/images/portfolio-site/rejected-v1.png" alt="AI 自己產出的版本" width="760" height="470" />
		</figure>

		<footer class="pb-foot" hidden>
			<p class="pb-note" data-slot="標註｜這張圖是什麼">這是 AI 給的第一版</p>
			<a class="pb-exit" href="/projects/ai/portfolio-site/" data-slot="出口文字">那我做了什麼 →</a>
		</footer>
	</article>
</section>

<style is:inline>
	.pb {
		display: flex;
		flex-direction: column;
	}

	/* 主角是名字——階 6，跟現況首頁同一個量級。公告不准壓過它。 */
	.pb-name {
		font-size: var(--fs-6);
		line-height: var(--lh-6);
		letter-spacing: var(--ls-display);
		font-weight: 400;
		margin: 0;
	}

	.pb-tagline {
		margin: var(--sp-xs) 0 0;
		color: var(--color-text-muted);
	}

	/* 卡片＝限界裝置。框與底把公告圈成「首頁上的一塊」。 */
	.pb-card {
		margin-top: var(--sp-2);
		padding: var(--sp-lg);
		background: var(--color-bg-alt);
		border: 1px solid var(--color-border-strong);
		border-radius: var(--radius);
		display: flex;
		flex-direction: column;
		gap: var(--sp-md);
		max-width: 46em;
	}

	/* 標籤沿用站上既有的「標籤」樣式（襯線 600 ＋ 主色）——那一組的辨識通道是
	   字體與字重，顏色只扛份量，符合「元素主色當文字色」的判準一與判準三。 */
	.pb-eyebrow {
		margin: 0;
		font-family: var(--font-serif);
		font-weight: 600;
		font-size: var(--fs-1);
		letter-spacing: var(--ls-label);
		color: var(--color-accent);
	}

	/* 問句降到內文級——它是區塊裡的內容，不是頁面的標題。 */
	.pb-ask {
		margin: 0;
	}

	.pb-gate {
		display: flex;
	}

	/* `hidden` 屬性靠的是 UA 樣式的 display:none，作者寫的 display:flex 會蓋掉它——
	   所以要顯式讓 [hidden] 贏。屬性選擇器提高一階特異度就夠，不必動用 !important。 */
	.pb-gate[hidden],
	.pb-foot[hidden],
	.pb-shot[hidden] {
		display: none;
	}

	.pb-btn {
		appearance: none;
		font: inherit;
		color: var(--color-text);
		background: transparent;
		border: 1px solid var(--color-border-strong);
		border-radius: var(--radius);
		padding: var(--sp-sm) var(--sp-lg);
		cursor: pointer;
		transition: background 200ms ease-out;
	}

	.pb-btn:hover {
		background: var(--color-bg-hover, var(--color-bg-alt));
	}

	.pb-shot {
		margin: 0;
		border: 1px solid var(--color-border);
		border-radius: var(--radius);
		overflow: hidden;
		max-height: 260px;
	}

	.pb-shot img {
		display: block;
		width: 100%;
		height: auto;
	}

	.pb-foot {
		display: flex;
		flex-wrap: wrap;
		align-items: baseline;
		justify-content: space-between;
		gap: var(--sp-sm);
	}

	.pb-note {
		margin: 0;
		color: var(--color-text-muted);
	}
</style>
